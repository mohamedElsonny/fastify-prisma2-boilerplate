# Migration `20200229161217-cuid-id`

This migration has been generated by Mohamed Gamal at 2/29/2020, 4:12:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE `blogpost`.`Post`,`blogpost`.`User`;

CREATE TABLE `blogpost`.`User` (
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `email` varchar(191) NOT NULL DEFAULT '' ,
    `id` varchar(191) NOT NULL  ,
    `name` varchar(191)   ,
    `password` varchar(191) NOT NULL DEFAULT '' ,
    `role` ENUM('USER', 'ADMIN') NOT NULL DEFAULT 'USER' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `blogpost`.`Post` (
    `author` varchar(191) NOT NULL ,
    `content` varchar(191)   ,
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `id` varchar(191) NOT NULL  ,
    `published` boolean NOT NULL DEFAULT false ,
    `title` varchar(191) NOT NULL DEFAULT '' ,
    `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `blogpost`.`User`(`email`)

ALTER TABLE `blogpost`.`Post` ADD FOREIGN KEY (`author`) REFERENCES `blogpost`.`User`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200229152429-added-password-to-user..20200229161217-cuid-id
--- datamodel.dml
+++ datamodel.dml
@@ -2,18 +2,18 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id        Int      @id @default(autoincrement())
+  id        String      @id @default(cuid())
   createdAt DateTime @default(now())
   email     String   @unique
   name      String?
   role      Role     @default(USER)
@@ -21,9 +21,9 @@
   password  String
 }
 model Post {
-  id        Int      @id @default(autoincrement())
+  id        String      @id @default(cuid())
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   author    User
   title     String
```


