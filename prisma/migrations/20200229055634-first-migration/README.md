# Migration `20200229055634-first-migration`

This migration has been generated by Mohamed Gamal at 2/29/2020, 5:56:34 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `blogpost`.`User` (
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `email` varchar(191) NOT NULL DEFAULT '' ,
    `id` int NOT NULL  ,
    `name` varchar(191)   ,
    `role` ENUM('USER', 'ADMIN') NOT NULL DEFAULT 'USER' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `blogpost`.`Post` (
    `author` int NOT NULL ,
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `id` int NOT NULL  ,
    `published` boolean NOT NULL DEFAULT false ,
    `title` varchar(191) NOT NULL DEFAULT '' ,
    `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `blogpost`.`User`(`email`)

ALTER TABLE `blogpost`.`Post` ADD FOREIGN KEY (`author`) REFERENCES `blogpost`.`User`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200229055634-first-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+
+model User {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  email     String   @unique
+  name      String?
+  role      Role     @default(USER)
+  posts     Post[]
+}
+
+model Post {
+  id         Int        @id
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @updatedAt
+  author     User
+  title      String
+  published  Boolean    @default(false)
+}
+
+enum Role {
+  USER
+  ADMIN
+}
```


