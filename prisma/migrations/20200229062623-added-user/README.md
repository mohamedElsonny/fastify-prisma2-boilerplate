# Migration `20200229062623-added-user`

This migration has been generated by Mohamed Gamal at 2/29/2020, 6:26:23 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `blogpost`.`User` (
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `email` varchar(191) NOT NULL DEFAULT '' ,
    `id` int NOT NULL  AUTO_INCREMENT,
    `name` varchar(191)   ,
    `role` ENUM('USER', 'ADMIN') NOT NULL DEFAULT 'USER' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `blogpost`.`Post` ADD COLUMN `author` int NOT NULL ;

CREATE UNIQUE INDEX `User.email` ON `blogpost`.`User`(`email`)

ALTER TABLE `blogpost`.`Post` ADD FOREIGN KEY (`author`) REFERENCES `blogpost`.`User`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200229062603-remove-user..20200229062623-added-user
--- datamodel.dml
+++ datamodel.dml
@@ -2,20 +2,30 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
+
+model User {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  email     String   @unique
+  name      String?
+  role      Role     @default(USER)
+  posts     Post[]
+}
+
 model Post {
   id        Int      @id @default(autoincrement())
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  // author    User
+  author    User
   title     String
   content   String?
   published Boolean  @default(false)
 }
```


